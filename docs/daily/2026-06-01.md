# 2026-06-01

## 会话摘要
确认数字高人矩阵首版范围：本地 Web App、后端 AI 代理、可控爬取、SQLite、无账号系统。

## 记录
- [决策] 架构按前端、后端、SQLite 三层拆分。
- [决策] 后端按 research/library/skill/evaluation/model 五个服务拆分。
- [决策] UI 按人物矩阵、任务工作区、证据输出三面板拆分。
- [约束] 爬取遵守 robots、限速、不登录第三方、不绕过付费墙。
- [进展] Task 2 建立 shared Zod schema 与 SQLite 七表迁移基础：people/sources/fragments/skills/evaluations/critiques/jobs。
- [经验] Vitest 配置引用 src/test/setup.ts；缺失 setup 会在数据库测试前阻断红绿循环，需先补最小测试环境入口。
- [进展] Task 3 建立 Express app/config/index、health/library 路由与 libraryService，people/sources/fragments SQL 收敛在服务层。
- [经验] libraryService 测试同时断言 shared schema parse 与 SQLite snake_case 行，能早期发现 camelCase 漂移。
- [进展] Task 4 建立 researchService 与 POST /api/research/crawl，支持种子 URL 抓取、正文抽取、URL/内容去重与 crawl job 落库。
- [经验] 片段去重要同时看 URL 和正文内容；source 负责 URL 归属，fragment 只保存抽取结果，状态才不会分叉。
- [进展] Task 5 建立 modelService、skillService、evaluationService 以及 skills/evaluations/critiques 路由，打通模型调用、skill 蒸馏、项目评审与 critique 持久化。
- [决策] `evaluations.person_id/skill_id` 作为快照字段保留，不对 people/skills 做硬外键；真实强约束只保留在 `evaluation_id` 这类不可替代关系上。
- [进展] Task 6 建立三面板前端工作台：People matrix、Workflow workspace、Evidence panel，并补齐 Vite `index.html`、API seam、组件目录与全局样式。
- [经验] 前端测试文案不要和旁侧说明复用完全相同的短语；测试会匹配冲突，界面语义也会变模糊。
- [进展] Task 7 为前端补齐 API seam 回归测试，验证 `App -> ResearchWorkspace -> apiClient` 的 crawl 提交流经。
- [经验] 前端纵向切片验收先看 API seam，再看真实浏览器布局；这样既能锁住交互接缝，也能避免把视觉问题误诊成状态问题。
- [验证] 独立 Chrome/Playwright 截图确认桌面端为左矩阵、中工作区、右证据栏三列；390px 窄视口自动折成单列，没有文本重叠或营销页占位。
- [进展] Task 8 建立 `e2e/` Playwright 模块与 `workflow-tabs.spec.ts`，在浏览器端逐个点击 `Research/Distill/Evaluate` 并附带每一步截图证据。
- [经验] 浏览器录证测试必须把 locator 收窄到 workflow tablist；否则 `Research` 这类词会和人物标签撞名，红测会误击错误节点。
- [经验] Vitest 与 Playwright 必须分离收集边界；否则 `npm test` 会把 e2e spec 当成单测导入，测试框架彼此打架。
- [进展] 根目录新增中文开源 README，系统说明项目定位、能力边界、快速开始、环境变量、测试策略与贡献约束。
- [进展] 修复 Evaluate 点击无反馈：模型响应支持 fenced JSON、前端补齐 running/error 状态、API client 透传后端错误正文，并新增 `MODEL_TIMEOUT_MS`。
- [经验] 真实模型不会像 mock 一样老实返回纯 JSON；代码块包裹、慢响应、超时都是常态，不处理这些现实噪声，产品就会表现成“按钮没反应”。
- [进展] 新增后端结构化日志链：`server/logger.ts` 统一输出 JSON 日志，`evaluationRoutes -> evaluationService -> modelService` 贯穿 `requestId`、请求上下文、模型预览、缺字段诊断与写库失败信息。
- [经验] 不要让 SQLite 的 `NOT NULL` 约束替你做业务校验；模型输出缺字段时，应在服务层先定位缺口并打出结构化上下文日志。
- [进展] 后端日志新增 `LOG_FILE_PATH` 落盘能力，默认写入 `logs/backend.ndjson`；评审 prompt 改为强制 JSON contract，明确要求 `personJudgment/businessJudgment/risks/questions/score` 等字段。
- [经验] 真实模型集成的正确做法是“强 prompt contract + 服务层字段校验 + ndjson 原始证据”，三者缺一项都会让排错变成猜谜。
- [进展] 前端完成中文优先改版：主标题、工作流、人物状态、Research/Distill/Evaluate 三个工作区与右侧证据面板全部切到中文语义。
- [进展] 前端布局改为 `100dvh` 三栏定高，桌面端整页不再纵向滚动，滚动收敛到左侧人物列表、中间工作区、右侧证据面板内部。
- [进展] 右侧证据面板新增单人物下拉切换，能在不切换左栏上下文的情况下快速审视不同人物的证据视角。
- [经验] 视口适配最稳的做法不是继续堆 `calc(100vh - x)`，而是让 `app-shell -> app-grid -> panel` 全链路都进入 `minmax(0, 1fr)` / `min-height: 0` 模式，把滚动封进局部容器。
- [经验] Testing Library 出现“同名控件命中多个节点”时，先排查测试环境是否缺 `cleanup()`；没有每测后清 DOM，后面的无障碍查询迟早会误报。
- [验证] `npm test -- src/test/App.test.tsx` 通过 4 个前端工作台测试；覆盖中文 tab 切换、crawl seam、评审 loading/error、右栏人物下拉切换。
- [验证] `npm run test:e2e -- e2e/workflow-tabs.spec.ts` 通过；新增浏览器断言确认右栏下拉能切到 `黄仁勋`，且桌面端 `document.scrollingElement.scrollHeight - clientHeight <= 4`，页面本身不再长滚。
- [验证] 全量验证 `npm test`、`npm run lint`、`npm run build`、`npm run test:e2e` 均通过。
- [进展] Task 2 收口 persona 库与 `nuwa-skill` 导入网关：导入按 canonical `origin_ref` 幂等，legacy `nuwa-skill:Paul Graham` 会在迁移时归一化并去重，`/api/personas` 与 `/api/people` 对软删除语义已对齐。
- [经验] 对“人物去重”这类身份归并，迁移不能只删重复主表行；所有引用该身份的快照表也要一起重写，否则历史记录会留下悬空语义。
- [进展] Task 3 建立 conversations/messages/conversation_runs 后端竖切：新增会话服务、run 编排服务、distillation import-first seam、conversation routes 与 app 装配。
- [经验] 会话系统的硬边界不是路由路径，而是消息与 run 的归属校验；`messageId` 与 `runId` 必须在服务层验证属于当前 `conversationId`，否则任何端点都能串线。
- [经验] 消息流如果按 `created_at + id` 排序，在同毫秒写入多条消息时顺序并不稳定；SQLite 本地工作台场景里，按 `rowid` 保持插入顺序更接近真实用户感知。
- [经验] Job 系统最忌讳“状态撒谎”；先落库为 `queued` 的任务，只要后续失败，就必须显式回写成 `failed`，不能把错误伪装成未开始。

## 验证
- [Task 1] `npm run lint` 处于预期空源码状态，TypeScript 输出：

```text
> lint
> tsc -p tsconfig.json --noEmit && tsc -p tsconfig.node.json --noEmit

error TS18003: No inputs were found in config file '/Volumes/SSD/code/brainpower/tsconfig.json'. Specified 'include' paths were '["src","shared"]' and 'exclude' paths were '[]'.
```
- [Task 2] `npm test -- server/test/db.test.ts` 先因 `../db/connection.js` 缺失失败，随后实现迁移后通过 1 个数据库 schema 测试。
- [Task 2] `npm run lint` 通过，`shared/schemas.ts` 让 `tsconfig.json` 具备真实输入。
- [Task 3] `npm test -- server/test/libraryService.test.ts` 先因 `../services/libraryService.js` 缺失失败，随后实现服务后通过 1 个资料库服务测试。
- [Task 3] `npm test -- server/test/app.test.ts` 先因 `../app.js` 缺失失败，随后实现 app/routes 后通过 1 个 HTTP 路由测试。
- [Task 4] `npm test -- server/test/researchService.test.ts` 先因 `../services/researchService.js` 缺失失败，随后实现采集服务后通过 1 个 researchService 测试。
- [Task 4] `npm test -- server/test` 通过 4 个测试文件共 7 个测试；`npm run lint` 通过。
- [Task 5] `npm test -- server/test/modelService.test.ts server/test/skillService.test.ts server/test/evaluationService.test.ts` 先因 3 个 service 文件缺失失败；实现后因 `evaluations` 外键过硬导致 `FOREIGN KEY constraint failed`，放松为快照字段后通过 3 个目标测试。
- [Task 5] `npm test -- server/test` 通过 7 个测试文件共 10 个测试；`npm run lint` 通过。
- [Task 6] `npm test -- src/test/App.test.tsx` 先因 `src/App` 缺失失败；实现后两次因重复文案导致 Testing Library 命中多个节点，去重文案后通过 1 个前端工作台测试。
- [Task 6] `npm run build` 先因 seedPeople 字面量类型过窄失败；放宽前端 seed 类型后 build 通过，`npm run lint` 通过。
- [Task 7] `npm test` 通过 8 个测试文件共 12 个测试；新增 `App` 的 crawl seam 测试后前后端回归仍稳定。
- [Task 7] `npm run lint`、`npm run build` 再次通过；`npx -y playwright screenshot --browser=chromium --channel=chrome` 生成桌面与 390px 窄视口截图，确认工作台布局成立。
- [Task 8] `npx playwright test e2e/workflow-tabs.spec.ts` 首次因缺失 `@playwright/test` 红测；补依赖后再次因 locator 命中多个 `Research` 按钮失败；收窄到 `Workflow tabs` 后通过。
- [Task 8] 全量验证 `npm test`、`npm run test:e2e`、`npm run lint`、`npm run build` 全部通过；其中 `vite.config.ts` 新增 `test.include`，避免 Vitest 误收 `e2e/*.spec.ts`。
- [Bugfix] `curl -X POST /api/evaluations` 先复现两类真实故障：模型 fenced JSON 导致 `Unexpected token '`'`，以及上游长等待导致 30s 无返回；修复后临时后端 `PORT=4181 MODEL_TIMEOUT_MS=5000` 会在 5s 返回 `{"error":"Model request timed out after 5000ms"}`。
- [Logging] `npm test -- server/test/evaluationService.test.ts` 新增红测，先复现 `NOT NULL constraint failed: evaluations.person_judgment`；修复后改为抛出 `Evaluation payload missing required fields: personJudgment`，并断言 `evaluation_payload_invalid` 日志包含 `requestId` 与 `missingFields`。
- [Logging] `npm test -- server/test/logger.test.ts` 验证日志落盘、长文本截断与敏感字段脱敏；`npm test` 通过 10 个测试文件共 19 个测试，`npm run build` 与 `npm run test:e2e` 通过。

## Retain
- → MEMORY: 分表、分服务、分 UI 面板是本项目核心约束。
- → bank/experience: 数字人物输出必须带证据、置信度和诚实边界，避免变成无来源拟人表演。
- → bank/world: SQLite 基础 schema 固定七张核心表：people、sources、fragments、skills、evaluations、critiques、jobs。
- → bank/experience: 若 Vitest setupFiles 指向缺失文件，业务测试会在收集前失败；先恢复测试入口再验证目标红测。
- → bank/experience: 后端路由保持薄层，SQL 只进入 service 文件；测试用 shared schema parse 防止数据库字段命名漂移。
- → bank/experience: researchService 用 URL 去重 + 正文 hash 去重的双重约束，比单纯堆更多 crawl_status 分支更稳。
- → bank/experience: 对评审记录这类快照数据，硬外键会把测试和演进都绑死；保留不可替代关系的外键，人物/技能引用改成快照字段更稳。
- → bank/experience: 前端 seed 数据若用 `as const` 直接喂给可变组件 props，TypeScript 很容易在 `readonly` 和字面量状态上绊住 build；工作台 seed 更适合显式宽类型。
- → bank/experience: 前端纵向切片验收先补 API seam 测试，再用独立浏览器截图确认桌面三列与窄视口单列；这样能把逻辑回归和布局回归分开验证。
- → bank/experience: Playwright 浏览器录证要先把 locator 锁进明确容器，再让 Vitest/Playwright 各自只收自己的测试文件；否则测试边界会互相污染。
- → bank/experience: 开源 README 必须写清“已实现什么、未实现什么、如何启动、如何验证、如何贡献”，否则首页会把原型项目伪装成成品。
- → bank/experience: 对真实模型集成，最少要处理三件事：markdown fenced JSON、超时、后端错误正文透传；否则前端故障表现只会是“没反应”。
- → bank/experience: 后端调试日志至少要覆盖 requestId、模型原始返回预览、缺字段列表、写库失败上下文；否则数据库报错只会给你症状，不会给你根因。
- → bank/experience: 模型输出 contract 要同时写进 prompt、服务层校验和日志证据；只靠 prompt 是愿望，只靠数据库约束是事故。
- → bank/experience: 桌面端想实现整屏工作台，关键不是继续算减法高度，而是让 `app-shell/app-grid/panel` 全部进入 `min-height: 0` 的内部滚动体系。
- → bank/experience: RTL 若未在每个用例后 `cleanup()`，同名输入控件会跨测试残留，`getByLabelText` 这类无障碍查询会产生假性多匹配。
- → bank/experience: 身份归并迁移不能只去重主表；凡是持有该身份语义的评审、批评、会话参与者与素材表都要一起重写，避免留下语义悬空的旧 ID。
- → bank/experience: 会话系统必须在服务层校验 `messageId/runId` 属于当前 `conversationId`；只靠 URL 路径无法保证边界。
- → bank/experience: SQLite 本地聊天流若需要稳定展示同轮多消息顺序，用 `rowid` 比 `created_at + id` 更稳。
